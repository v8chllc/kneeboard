<!-- remember-journal
source: manual
kind: session
session_hash: 01caccfa
captured_at: 2026-07-27T22:04:30.8211396-05:00
window_start: 2026-07-27T19:51:45-05:00
window_end: 2026-07-27T22:04:30.8211396-05:00
-->

## 22:04 Session

### What happened

Resolved a transient Codex `node_repl` startup warning by confirming the managed
runtime existed and restarting Codex. Loaded project context and memory, then
completed the remaining SimBrief capture scenarios: southern/eastern hemisphere,
missing detailed navlog, unsupported layout, and sparse optional flight identity.

Documented the exact normalized SimBrief field mapping across five valid captures
and 132 navlog rows. Corrected the earlier assumption that coordinates were JSON
numbers: every required numeric-looking value was a quoted string. Recorded the
approved fail-closed coordinate and SID/STAR classification rules in the
governing behavior documents.

Added a deterministic allowlisting sanitizer and seven tracked raw-shaped parser
fixtures: five valid scenarios and two rejection scenarios. Marked capture,
mapping, and sanitization tasks complete.

### Key context

Raw OFPs remain ignored under `.local/simbrief/`. Tracked fixtures retain only
approved parser fields, public route/procedure identifiers, and the deliberately
retained simulator registration `N101SB`; ordinary identity, coordinates,
distances, and timestamps are synthetic. Application scaffolding and Vitest do
not exist yet.

### Decisions considered

- Display `general.flight_number` unchanged, including SimBrief's substitution
  of simulator registration when airline and flight number are blank.
- Allowlist coordinate `ltlg` fixes with `^\d{2}[NS]\d{3}[EW]$`; fail closed for
  every other `ltlg` row.
- Classify SID/STAR only for `wpt` or `vor` rows whose flag is true and whose
  inbound airway matches exactly one dedicated procedure identifier; ambiguous
  rows fail closed.
- Use minimal upstream-shaped fixtures rather than copied full OFPs or
  pre-normalized domain objects.

### Blockers

None. Six unrelated implementation-planning choices remain open in
`docs/planning-status.md`.

### Next steps

Create synthetic edge-case fixtures for exactly 9/10 eligible fixes, excluded
page-boundary rows, coordinate rollover, malformed numeric values, missing
fields, and repeated identifiers. Then resolve the remaining planning choices
before starting the framework-independent domain layer.

### References

- `docs/tracker-behavior.md`
- `docs/simbrief-navlog-findings.md`
- `docs/planning-status.md`
- `docs/task-list.md`
- `scripts/sanitize_simbrief_fixture.py`
- `tests/fixtures/simbrief/README.md`
